import { Panel } from "@/shared/ui/Panel";

export function GmRoomsPage() {
  return (
    <section className="page">
      <div>
        <div className="page-title">Rooms</div>
        <div className="page-subtitle">Fast room scanning and selection.</div>
      </div>
      <Panel title="Rooms" subtitle="Bootstrap placeholder">
        Rooms list view placeholder.
      </Panel>
    </section>
  );
}
